# FIXES — implementation log for REVIEW.md findings

Worked in REVIEW.md's suggested fix order. Owner input applied: "not deployed
yet, breaking changes allowed" — this resolved R-002 to Option A (no field
boards exist; code now matches the documented layout) and authorized the
breaking API changes REVIEW.md had flagged as owner decisions.

Verification ran via the host unit-test suite added at `test/host` (mock I2C
with faithful 24AA02E64 page-buffer semantics, fault injection, and a
bus-concurrency canary), since no ESP-IDF install or hardware is available on
this machine — REVIEW.md's stated no-hardware verification path. Full suite:
`cd test/host && make` → **ALL TESTS PASSED, 0 failures**, zero compiler
warnings with `-Wall -Wextra -Werror=format`.

- **R-002** — DONE (Option A: no field boards). Component I/O moved to `CAP_OFFSET_COMPONENTS` (16); 64-bit little-endian timestamp now written to and read from bytes 8–15; `CAP_OFFSET_IC_LIST` kept as deprecated alias of `CAP_OFFSET_COMPONENTS`; header comments document endianness. Files: `include/esp_hardware_discovery.h`, `src/esp_hardware_discovery.c`. Verified: `grep -n CAP_OFFSET src/` shows only `CAP_OFFSET_COMPONENTS` for component I/O, matching the memory-map docs; `test_r001_r002_write_layout` asserts the byte-exact on-chip image (magic@0, count@7, LE timestamp@8–15, descriptors@16) and timestamp round-trip — PASS.
- **R-001** — DONE. `eeprom_write_bytes` now chunks every write to ≤8 bytes per transaction, never crossing an 8-byte page boundary, with ACK polling (`eeprom_wait_write_complete`, ~6 ms timeout via probe) after each page replacing the fixed 5 ms delay. Files: `src/esp_hardware_discovery.c`. Verified: `test_r001_r002_write_layout` asserts every logged write transaction has `len ≤ 8` and `(mem_addr % 8) + len ≤ 8`, and the 5-component board round-trips byte-exact through the mock's real page-wraparound semantics (which would corrupt unchunked writes) — PASS.
- **R-004** — DONE. `eeprom_write_capabilities` commits descriptors first, then the timestamp page, then the header page carrying the magic byte strictly last, and verifies header + descriptors by read-back (memcmp) before reporting success; force-reprogram caveat documented in the header doc comment. Files: `src/esp_hardware_discovery.c`, `include/esp_hardware_discovery.h`. Verified: `test_r004_commit_order` asserts transaction order (descriptors < timestamp < header, header last) and that a fault-injected descriptor-write failure leaves magic erased (0xFF) with `eeprom_is_programmed() == false` — PASS.
- **R-003** — DONE. Replaced the boolean guard with `eeprom_get_prog_state()` (blank / programmed / partial-invalid / bus-error); with `force=false` a bus error or dirty image aborts the write with ESP_LOGE. Public `eeprom_is_programmed()` signature and semantics remain compatible. Files: `src/esp_hardware_discovery.c`. Verified: `test_r003_guard` fault-injects a magic-read failure and asserts the write is refused with zero write transactions issued, data intact, and `force=true` still works — PASS.
- **R-006** — DONE. `caps->is_valid` is now set only after every read (header, descriptors, footer, UID) has completed; the descriptor-failure path leaves it false. Files: `src/esp_hardware_discovery.c`. Verified: `test_r006_partial_read` fault-injects the descriptor read and asserts return false, `is_valid == false`, and that `eeprom_has_ic`/`eeprom_count_category`/`eeprom_find_category` all treat the struct as invalid — PASS.
- **R-009** — DONE. Migrated to the `i2c_master` driver API (`i2c_master_transmit`, `i2c_master_transmit_receive`, `i2c_master_probe`); added public `eeprom_discovery_init(i2c_master_bus_handle_t)` with lazy per-address device handles (0x50–0x57 only, at `EEPROM_DISCOVERY_I2C_SPEED_HZ`, default 100 kHz); all bus functions error cleanly before init; `idf_component.yml` bumped to `idf >= 5.2.0`; README init section rewritten for the new API; examples' `app_main` now creates the bus and calls init. Downstream-pin investigation was moot (owner: nothing deployed). Files: `src/esp_hardware_discovery.c`, `include/esp_hardware_discovery.h`, `idf_component.yml`, `README.md`, `esp_hardware_discovery_examples.c`. Verified: `test_r009_uninitialized_and_init` asserts every bus function refuses to run before init (zero bus traffic), `init(NULL)` → `ESP_ERR_INVALID_ARG`, double init → `ESP_ERR_INVALID_STATE`; whole suite runs on the new API — PASS. (On-target `idf.py build` deprecation-warning check still recommended when an IDF 5.2+ toolchain is available.)
- **R-008** — DONE (subsumed by R-009 as REVIEW.md anticipated). The hardcoded `I2C_NUM_0` is gone; the bus is whatever handle the app passes to `eeprom_discovery_init()`, so any port/bus works. Files: as R-009. Verified: no `I2C_NUM_0` reference remains (`grep -rn I2C_NUM_0 src include` → empty); mock bus exercises the handle path — PASS.
- **R-015** — DONE (made moot by R-009, the resolution REVIEW.md preferred). The legacy command-link builder calls no longer exist; every `i2c_master_*` return value is checked and propagated. Files: `src/esp_hardware_discovery.c`. Verified: code inspection — no unchecked I2C calls remain; clean build — PASS.
- **R-019** — DONE (the R-009 variant): `REQUIRES esp_driver_i2c`, which is correct as a *public* dependency now that the public header includes `driver/i2c_master.h` for the init prototype. Files: `CMakeLists.txt`, `include/esp_hardware_discovery.h`. Verified: dependency matches the header's include; full on-target consumer build pending an IDF toolchain — PASS (host-verifiable part).
- **R-007** — DONE (option a, as recommended). Module-level FreeRTOS mutex created in `eeprom_discovery_init()`; every public bus-touching function is a lock-taking wrapper around static unlocked internals (no recursive locking, no nested public calls); thread-safety contract documented in the header. Files: `src/esp_hardware_discovery.c`, `include/esp_hardware_discovery.h`. Verified: `test_r007_concurrency` — two pthreads × 200 status updates each on different ICs; the mock's atomic concurrency canary recorded 0 overlapping bus entries, both final statuses correct, all writes page-safe — PASS.
- **R-014** — DONE. Both low-level functions reject `mem_addr + len > 256` with `ESP_ERR_INVALID_SIZE`; writes overlapping the factory unique-ID region (≥ 0xF8) are rejected with `ESP_ERR_INVALID_ARG` (the reserved footer 240–247 stays writable). Files: `src/esp_hardware_discovery.c`. Verified: `test_r014_bounds` calls the statics directly (test includes the .c): OOB write/read → `ESP_ERR_INVALID_SIZE`, UID-overlap write → `ESP_ERR_INVALID_ARG` with zero bus transactions, footer write and UID read still allowed — PASS.
- **R-012** — DONE. Unique-ID read result checked; on failure logs `ESP_LOGW` and continues (capability read still succeeds, UID stays zeroed). Files: `src/esp_hardware_discovery.c`. Verified: `test_r012_uid_failure` fault-injects the UID read: read returns true, `is_valid == true`, UID all zeros — PASS.
- **R-016** — DONE. `%zu` for both `size_t` allocation logs; timestamp print switched to `PRIu64` (opportunistic, per spec) with `<inttypes.h>`. Files: `src/esp_hardware_discovery.c`. Verified: entire suite builds with `-Werror=format` and zero warnings — PASS.
- **R-005** — DONE. Examples fixed: include is now `esp_hardware_discovery.h`, `PROJECT_GALMON`/`GALMON_PCB_MAIN` → `PROJECT_GNSS`/`GNSS_PCB_MAIN`, FreeRTOS includes added, stale `@file` corrected, "reference only / defines app_main" comment added (kept at repo root — the spec's optional `examples/` project restructure not taken, would be unbuildable-here scaffolding). **Extension found during verification:** the `IC()` helper macro's `id` parameter captured the `.id` struct designator, so every `IC_*` macro failed to compile for any consumer (invisible until now because nothing ever compiled them); renamed the parameter to `ic_id`. Files: `esp_hardware_discovery_examples.c`, `include/esp_hardware_discovery.h`. Verified: `make syntax-examples` (clang `-fsyntax-only` against the mock ESP-IDF headers) → zero errors, zero warnings — PASS.
- **R-010** — DONE. Example 1 now programs `IC_EEPROM_SELF` at `components[0]` (count 6→7) and validates the self-reference after the verify read; Example 6 same treatment (count 3→4); Example 2's init loop starts at index 1 with the standard comment. Files: `esp_hardware_discovery_examples.c`. Verified: syntax check passes; the identical fixture shape (self-ref at [0]) passes `eeprom_validate_self_reference()` in `test_r001_r002_write_layout` — PASS.
- **R-011** — DONE (keep-but-warn variant; both were in-spec, and warn preserves the scan's return semantics while still surfacing the problem — the field-board concern that motivated "needs investigation" is moot since nothing is deployed). `eeprom_scan_bus` validates each read device's self-reference and logs `ESP_LOGW "... no valid self-reference - foreign EEPROM?"` on failure, still counting the device. Files: `src/esp_hardware_discovery.c`. Verified: `test_r011_scan_foreign` — valid board at 0x50, foreign device (magic 42, garbage body) at 0x51, erased device at 0x52: scan returns 2, foreign entry present and failing validation, erased not counted — PASS.
- **R-013** — DONE (preferred option, authorized by "breaking changes allowed"). `eeprom_find_category()` returns `const eeprom_ic_descriptor_t*`; the cast-away-const is gone; Example 7 updated; noted in CHANGELOG. Files: `include/esp_hardware_discovery.h`, `src/esp_hardware_discovery.c`, `esp_hardware_discovery_examples.c`, `CHANGELOG.md`. Verified: clean compile of library, tests, and examples with the const signature — PASS.
- **R-017** — DONE. First-match behavior documented on `eeprom_update_ic_status()`; added `eeprom_update_ic_status_at()` (matches category + id + address) sharing one internal implementation; legacy behavior unchanged. Files: `include/esp_hardware_discovery.h`, `src/esp_hardware_discovery.c`, `README.md`. Verified: `test_r017_duplicates` — two MCP9808s at 0x18/0x19: `_at` updates only the intended one, legacy call updates the first, wrong-address `_at` is refused — PASS.
- **R-018** — DONE. `reserved_footer` now populated from bytes 240–247 on every capability read (warn-and-continue on failure, matching the UID pattern); `CAP_COMPONENT_SIZE` removed (unused duplicate — deletion authorized by owner's breaking-changes go-ahead); `CAP_OFFSET_IC_LIST` deprecated alias per R-002; `CAP_OFFSET_FOOTER` comment corrected (8-byte reserved area within the 16-byte footer region). Files: `include/esp_hardware_discovery.h`, `src/esp_hardware_discovery.c`, `CHANGELOG.md`. Verified: `test_r018_footer` round-trips a footer pattern into `caps.reserved_footer` — PASS; grep confirms every remaining layout macro is either used or explicitly marked deprecated/informational.
- **R-020** — DONE except one sub-item. Fixed: capacity 60→56 and 240→224 bytes (README + CHANGELOG); phantom CHANGELOG deliverables ("Manufacturing programmer with LED feedback", "Self-provisioning module") removed; OVERVIEW.md written (CLAUDE.md reference now resolves); README clone URL corrected to `esp32-hardware-discovery` matching the actual `origin` remote; README file-structure updated; write-time claim now consistent with the paged implementation. **SKIPPED sub-item:** the ESP Component Registry coordinates (`ptudor/esp_hardware_discovery`) and the `ptudor.net` URLs in `idf_component.yml` can't be verified from here — whether the registry name should match the repo name is a publishing decision only the owner can make. Files: `README.md`, `CHANGELOG.md`, `OVERVIEW.md`. Verified: `grep -rn '60\|OVERVIEW'` across the docs shows no stale claims; clone URL matches `git remote -v` — PASS (except the skipped registry-naming item).
